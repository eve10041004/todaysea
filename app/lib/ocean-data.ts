export type OceanSnapshot = { date: string; weather?: string; temperature?: number; rainProbability?: number; rainAmount?: string; windSpeed?: number; windDirection?: string; waveHeight?: number; warning?: string; source: string; available: boolean; error?: string };

export function normalizeKmaForecast(date:string,items:Array<{category:string;fcstValue:string}>):OceanSnapshot{const value=(category:string)=>items.find(item=>item.category===category)?.fcstValue;const sky=value("SKY"),pty=value("PTY");const weather=pty&&pty!=="0"?`강수 코드 ${pty}`:sky==="1"?"맑음":sky==="3"?"구름 많음":sky==="4"?"흐림":undefined;const wind=value("WSD"),rain=value("POP"),amount=value("PCP");return{date,weather,temperature:value("TMP")?Number(value("TMP")):undefined,rainProbability:rain?Number(rain):undefined,rainAmount:amount,windSpeed:wind?Number(wind):undefined,windDirection:value("VEC")?`${value("VEC")}°`:undefined,source:"기상청 단기예보",available:Boolean(items.length)};}

export function calculateSuitability(snapshot:OceanSnapshot,mode:"haenyeo"|"fisher"){if(!snapshot.available)return{score:null,label:"예보 정보 없음"};let score=mode==="haenyeo"?84:86;if((snapshot.windSpeed??0)>8)score-=mode==="haenyeo"?20:15;else if((snapshot.windSpeed??0)>5)score-=8;if((snapshot.waveHeight??0)>1.5)score-=mode==="haenyeo"?20:15;if((snapshot.rainProbability??0)>60)score-=8;return{score:Math.max(0,Math.min(100,score)),label:score>=75?"적합":score>=55?"주의":"비추천"};}
